当实体有跟随运动器时，如果尝试获取实体位置，会发现获取到的位置并不是实体看起来的位置，而是该实体**创建时的位置**。

因此不能通过获取实体位置来获取有跟随运动器的实体的位置，这样就会总是获取到初始位置。可以通过获取该实体**跟随的实体**位置来规避这个问题。


详细情况描述见视频：[@Biliblli](https://www.bilibili.com/video/BV1tR19BLEYn)


